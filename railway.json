{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Saltando compilación de Next.js / Prisma: Web Estática Detectada'"
  },
  "deploy": {
    "startCommand": "npx -y serve -l $PORT"
  }
}
